<h1>Customers</h1>

<ul>
  <% @orders.each do |order| %>
     <table style="width:100%">
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Email</th>
  </tr>
  <tr>
    <td><%= order.first_name %></td>
    <td><%= order.last_name %></td>
    <td><%= order.email %></td>
  </tr>
</table> 
  <% end %>
</ul>