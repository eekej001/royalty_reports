<%= link_to("<< Back to Order List", {:action => 'index'}, :class => 'back-link') %>

<div class="orders delete">
  <h2>Delete Order</h2>

  <%= form_for(:order, :url => {:action => 'destroy', :id => @order.id}) do |f| %>

    <p>Are you sure you want to permanently delete this order?</p>
    
    <div class="form-buttons">
      <%= submit_tag("Delete Order") %>
    </div>
  <% end %>
</div>   